
env = Environment()
env.Append(CPPPATH = '.', CCFLAGS = '-g')
#env.SharedLibrary(target = 'CmdLineParser', source = 'CmdLineParser.cc')
env.StaticLibrary(target = 'CmdLineParser', source = 'CmdLineParser.cc')
